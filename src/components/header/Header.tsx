import { NavBar } from './NavBar';
import PromotionsBar from './PromotionsBar';

const Header = () => {
  return (
    <header>
      <PromotionsBar />
      <NavBar />
    </header>
  );
};

export default Header;
